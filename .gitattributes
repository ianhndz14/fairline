# Store text files with LF line endings in the repo and in working copies, on every OS.
* text=auto eol=lf

# Windows scripts need CRLF to run.
*.cmd text eol=crlf
*.bat text eol=crlf

*.png binary
*.jpg binary
*.pdf binary
